{
  "buildCommand": "cd workspace/web-app && npm run build",
  "outputDirectory": "workspace/web-app/dist",
  "installCommand": "cd workspace/web-app && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
